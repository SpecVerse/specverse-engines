{
  "name": "specverse-engines",
  "version": "3.5.2",
  "private": true,
  "description": "SpecVerse engine packages — parser, inference, realize, entities, generators",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
