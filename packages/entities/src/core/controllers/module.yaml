name: controllers
type: core
version: 3.5.1
depends_on: [models]

facets:
  schema: schema/controllers.schema.json
  conventions:
    structural: conventions/controller-processor.ts
    behavioural: behaviour/conventions/grammar.yaml
  behaviour:
    rules: behaviour/rules.qnt
    invariants: behaviour/invariants.qnt
  inference:
    entry: inference/index.ts
  generators: generators/index.ts
  docs: docs/index.ts
  tests: tests/index.ts
  examples: examples/

diagrams:
  - type: architecture

delivery:
  parser: true
  inference: false
  realize: true
  cli: true
