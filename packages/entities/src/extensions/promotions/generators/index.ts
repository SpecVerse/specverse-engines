import type { EntityGenerator } from '../../../_shared/types.js';

// Promotion generators — documentation generation from promotion definitions
export const promotionGenerators: EntityGenerator[] = [];
