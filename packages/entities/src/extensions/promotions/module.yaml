name: promotions
type: extension
version: 0.1.0
depends_on: [models]

facets:
  schema: schema/promotions.schema.json
  conventions:
    structural: conventions/promotion-processor.ts
    behavioural: behaviour/conventions/grammar.yaml
  behaviour:
    rules: behaviour/rules.qnt
    invariants: behaviour/invariants.qnt
  inference:
    entry: inference/index.ts
  generators: generators/index.ts
  docs: docs/index.ts
  tests: tests/index.ts
  examples: examples/

diagrams: []

delivery:
  parser: true
  inference: true
  realize: true
  cli: false
