/**
 * Measures Inference Rules
 *
 * No inference rules yet. Future rules may auto-generate measures
 * from model conventions (e.g., monetary fields → revenue measures)
 * or infer dimensions from model relationships.
 */

import type { EntityInferenceRule } from '../../../_shared/types.js';

export const measureInferenceRules: EntityInferenceRule[] = [];
