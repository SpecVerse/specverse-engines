import type { EntityGenerator } from '../../../_shared/types.js';

// Generators will be added when instance factories for CLI frameworks are created
// Future targets: Commander.js, Click (Python), Cobra (Go)
export const commandGenerators: EntityGenerator[] = [];
