/**
 * Conventions Entity - Inference Rules
 *
 * Convention definitions don't trigger inference rules yet.
 * Future: convention → attribute processor rule, convention → validation rule
 */

import type { EntityInferenceRule } from '../../../_shared/types.js';

export const conventionInferenceRules: EntityInferenceRule[] = [];
