/**
 * Conventions Entity - Generators
 *
 * Future: generate convention processor code from convention definitions.
 * This would enable fully declarative convention definitions that
 * auto-generate the TypeScript processing logic.
 */

import type { EntityGenerator } from '../../../_shared/types.js';

export const conventionGenerators: EntityGenerator[] = [];
